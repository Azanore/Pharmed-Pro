# PharmEd Pro - Environment Variables
# This application runs entirely locally with NO external API dependencies.
# No API keys or secrets are required for operation.

# APP_URL: Optional - The URL where this application is hosted.
# Used for deployment configurations only.
APP_URL="http://localhost:3000"
